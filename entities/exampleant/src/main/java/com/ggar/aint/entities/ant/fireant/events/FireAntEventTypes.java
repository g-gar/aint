package com.ggar.aint.entities.ant.fireant.events;

import com.ggar.aint.entities.common.event.EventType;

public enum FireAntEventTypes implements EventType {
	VIBRATIONS_DETECTED,
	AIR_CURRENTS_VARIATION_DETECTED,
	LIGHT_VARIATION_DETECTED,
	TEMPERATURE_VARIATION_DETECTED,
	CHEMICAL_TRAIL_DETECTED,
	ALLY_ANT_DETECTED,
	ENEMY_ANT_DETECTED
}
